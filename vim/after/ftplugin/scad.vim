" Add Render command to open design in OpenSCAD
command! Render execute "!openscad % $"

" Use C syntax highlighting
" Not ideal but good enough
set syntax=c

" May as well keep same formatting as OpenSCAD editor
set noexpandtab
