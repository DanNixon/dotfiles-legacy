---
- name: Ensure packages are installed
  become: true
  ansible.builtin.package:
    name:
      - base-devel
      - devtools
      - direnv
      - dnsutils
      - fzf
      - htop
      - ipcalc
      - jq
      - nnn
      - progress
      - qrencode
      - rclone
      - rsync
      - sipcalc
      - shellcheck
      - sshfs
      - sshuttle
      - termdown
      - tree
      - udisks2
      - unzip

      - ffmpeg
      - go
      - hugo

    state: present
