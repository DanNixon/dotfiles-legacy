---
- name: Ensure packages are installed
  become: true
  ansible.builtin.package:
    name:
      - base-devel
      - devtools
      - direnv
      - dnsutils
      - fzf
      - htop
      - ipcalc
      - jq
      - nnn
      - progress
      - qrencode
      - rclone
      - rsync
      - sipcalc
      - shellcheck
      - sshfs
      - sshuttle
      - termdown
      - tree
      - udevil
      - unzip

      - ffmpeg
      - go
      - hugo

    state: present
