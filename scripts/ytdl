#!/bin/bash

set -xe

url="$(xclip -o)"

format="$(rofi-youtube-dl-select-format "$url")"

youtube-dl --format="$format" "$url" \
  && notify-send 'youtube-dl' 'Download complete.' \
  || notify-send 'youtube-dl' 'Download failed!'
